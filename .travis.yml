language: node_js
node_js:
  - "4.0.0"
before_script:
  - npm install grunt-cli -g
  - npm install firebase-tools -g
  - npm install bower -g
  - npm install
  - bower install
  - grunt build
  - firebase deploy --token ${FIREBASE_TOKEN}
